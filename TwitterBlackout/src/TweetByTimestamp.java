
import java.util.Comparator;


/**
 * Comparator class to compare two tweets by their timestamp.
 * @author Samantha
 */
public class TweetByTimestamp implements Comparator<Tweet> {
    
    public int compare(Tweet t1, Tweet t2) {
        //need if timestamp is a real time stamp rather than a string
        return 0;
    }
}
